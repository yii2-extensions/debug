{
  "name": "@yii2-extensions/debug-assets",
  "private": true,
  "type": "module",
  "description": "Build pipeline for the yii2-extensions/debug client-side assets.",
  "scripts": {
    "build": "vite build",
    "dev": "vite build --watch",
    "lint:css": "stylelint --config ../../.github/linters/.stylelintrc.json --config-basedir . 'src/styles/*.css'",
    "lint:css:fix": "stylelint --config ../../.github/linters/.stylelintrc.json --config-basedir . --fix 'src/styles/*.css'"
  },
  "engines": {
    "node": ">=20.10"
  },
  "devDependencies": {
    "stylelint": "^17.10.0",
    "stylelint-config-standard-scss": "^17.0.0",
    "vite": "^8.0.0"
  }
}
